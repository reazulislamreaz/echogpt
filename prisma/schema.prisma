// EchoGPT Backend — Prisma schema foundation.
// Business models (User, Role, Session, Subscription, Provider, Chat, Search, Usage)
// will be introduced in later steps. Keep this file minimal until then.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
