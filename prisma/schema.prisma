// Prisma schema for EchoGPT backend.
// Models will be added in subsequent iterations.
// Domains: User, Role, Session, Subscription, Provider, Chat, Search, Usage

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
